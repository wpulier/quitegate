// Template for local secrets. Copy to Secrets.xcconfig (gitignored) and fill in:
//   cp Secrets.example.xcconfig Secrets.xcconfig
// CLERK_PUBLISHABLE_KEY is a Clerk *publishable* key (pk_test_/pk_live_) — client-side, safe to embed.
// NEVER put the Clerk secret key (sk_...) here; that is backend-only.
CLERK_PUBLISHABLE_KEY =
